spring:
  datasource:
    url: jdbc:mysql://localhost:3306/allo?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    database-platform: org.hibernate.dialect.MySQLDialect

jwt:
  secret: your-256-bit-secret-key-here-must-be-at-least-32-chars
  expiration: 86400000

server:
  port: 8080
